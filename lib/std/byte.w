pub base[1] class Byte {

    constructor() {
        test: string = 'test'
        {
            res = this.abc(test)
        }
        if test != 'test' {
            abc = 'abc'
        } else {
            eprintln()
        }
    }

} alias byte
